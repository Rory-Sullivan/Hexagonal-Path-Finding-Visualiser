describe('grid matrix tests', function() {
    it('iterate over array', function() {
        let access;
        for (let i = 0; i < m; i++) {
            for (let j = 0; j < n; j++) {
                access = grid[i][j];
            }
        }
    });
});
